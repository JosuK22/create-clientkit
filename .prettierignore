dist
node_modules

# Prettier cannot infer a parser for extension-less template files, and they
# are plain text anyway. Everything else under templates/ is formatted.
templates/astro-tailwind/base/_gitignore
templates/astro-tailwind/base/_gitattributes

# Generated by scripts/generate-third-party.mjs - that script owns its format.
THIRD-PARTY.md

# Verbatim captures of the head a real `astro build` emitted. Reformatting them
# would make them a rendering of the output rather than the output, which is
# the one property that makes them worth asserting against.
test/fixtures/

# Stage briefs, kept exactly as they were handed over. Reformatting a
# specification would make it a rendering of the instruction rather than the
# instruction, and these are read as evidence of what a stage was asked for.
docs/stages/
