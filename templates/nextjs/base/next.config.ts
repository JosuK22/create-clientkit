import type { NextConfig } from 'next';

/**
 * Deliberately empty.
 *
 * Every option Next offers here is a decision about deployment, images or
 * experimental behaviour, and none of those can be made correctly before the
 * project has a host. An empty config is the honest starting point; add to it
 * when there is something to add.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
